## Bandergames landing page

#### Deployment

```bash
# we use `serve` to serve the app
npm install -g serve
# to make sure your user has has permissions to execute the file
chmod +x ./server.sh
# run deploy script
npm run deploy
```
